import 'package:flutter/material.dart';

class AndroidAR extends StatelessWidget {
  const AndroidAR({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text(''),
    );
  }
}
